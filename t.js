const Person = require('./one'); // Import the Person class from person.js

// Create an instance of the Person class
const p = new Person('Alice', 30);

// Call the introduceYourself method
p.introduceYourself();
